# Direct dependencies only. Was a full `pip freeze` of 42 transitive packages,
# which pinned things like xerces bindings that this project never imports.
matplotlib==3.10.5
numpy==2.3.2
pandas==2.3.2
scikit-learn==1.7.1
torch==2.8.0

# ONNX export and verification (export_onnx.py, verify_inference.py)
skl2onnx==1.20.0
onnxruntime==1.30.0
joblib==1.5.2
uproot==5.6.7

# Pinned deliberately. skl2onnx emits `nodes_missing_value_tracks_true` as
# numpy bools, which onnx's make_attribute rejects for an int-list attribute.
# The attribute only appears when the trees have NaN-handling splits, i.e.
# exactly our case. export_onnx.py carries a scoped workaround; keep this pin
# until skl2onnx fixes it upstream, then drop both.
onnx==1.18.0
